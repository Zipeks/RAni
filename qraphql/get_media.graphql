query GetMedia(
    $season: MediaSeason
    $seasonYear: Int
    $type: MediaType!
    $format: MediaFormat
    $sort: [MediaSort]
    $search: String
    $page: Int
    $perPage: Int
    $status: MediaStatus
) {
    Page(page: $page, perPage: $perPage) {
        pageInfo {
            currentPage
            hasNextPage
            perPage
            total
            lastPage
        }
        media(
            season: $season
            seasonYear: $seasonYear
            type: $type
            format: $format
            sort: $sort
            search: $search
            status: $status
        ) {
            averageScore
            id
            title {
                userPreferred
                romaji
                english
                native
            }
            type
            episodes
            chapters
            nextAiringEpisode {
                episode
                timeUntilAiring
            }
        }
    }
}
